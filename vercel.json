{
  "installCommand": "npm ci && npm --prefix frontend ci",
  "buildCommand": "VITE_PUBLIC_SHOWCASE_ONLY=1 npm run build:frontend",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
