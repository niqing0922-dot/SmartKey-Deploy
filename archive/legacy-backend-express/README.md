# Legacy Express Backend

This directory preserves the pre-rebuild Express/JWT backend for reference only.

The active SmartKey backend is the FastAPI application under `backend/main.py`, `backend/routers`, and `backend/services`.

Do not use this legacy backend for new work. It contains the old login-first model and protected routes, which conflict with the local-first rebuild target.
